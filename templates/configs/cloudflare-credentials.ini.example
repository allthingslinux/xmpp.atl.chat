# Cloudflare API credentials for Let's Encrypt DNS-01 challenges
# Copy this file to ../cloudflare-credentials.ini and fill in your credentials

# Option 1: Global API Key (legacy, not recommended)
# dns_cloudflare_email = your-email@example.com
# dns_cloudflare_api_key = your-global-api-key

# Option 2: API Token (recommended)
# Create a token at https://dash.cloudflare.com/profile/api-tokens
# with Zone:Zone:Read and Zone:DNS:Edit permissions for your domain
dns_cloudflare_api_token = your-api-token-here

# Security note: Keep this file secure and never commit it to version control 